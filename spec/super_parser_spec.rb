# frozen_string_literal: true

RSpec.describe SuperParser do
  it "has a version number" do
    expect(SuperParser::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
